import React from "react"
import ReactDOM from "react-dom"

import { App } from "./App"
const rootElement = document.getElementById("root")

//ahora dibujamos(que dibujamos,donde dibujamos)
ReactDOM.render(<App />, rootElement)